version: 2.1
orbs:
  cypress: cypress-io/cypress@1
jobs:
  deploy:
    macos:
      xcode: 11.4.1
    steps:
      - run:
         command: curl -X POST -d {} ${NETLIFY_BUILD_HOOK_URL}

workflows:
  build:
    jobs:
      - cypress/run:
          yarn: true
      - deploy:
          requires:
            - cypress/run
